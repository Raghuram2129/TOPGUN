#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	cin>>n;
	int arr[n];
	bool sorted=true;
	for(int i=0;i<n;i++)
	{
		cin>>arr[i];
	}
	for(int i=1;i<n;i++)
	{
		if(arr[i]<arr[i-1])
		sorted=false;
	}
	if(sorted)
	cout<<"true\n";
	else
	cout<<"false";
}
